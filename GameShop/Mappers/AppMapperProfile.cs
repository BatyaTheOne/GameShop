using AutoMapper;
using GameShop.DTO;
using GameShop.Models;

namespace GameShop.Mappers
{
    //public class AppMapperProfile : Profile
    //{
    //    public AppMapperProfile()
    //    {
    //        CreateMap<Game, GameDto>().ReverseMap()
    //            .ForMember(dest => dest.GameTypeId, opt => opt.MapFrom(src => (int)src.Type))
    //            .ForMember(dest => dest.LibraryId, opt => opt.MapFrom(src => 1))
    //            .ForMember(dest => dest.MinimumLimitAge, opt => opt.MapFrom(src => src.AgeLimit));
    //    }
    //}
}
